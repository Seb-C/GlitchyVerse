/**
 * Each screen is a property of this object. Each screen has the following functions : 
 * - drawIcon(controlScreen, x, y, width, height)   --> Draws the icon of the screen in the given area
 * - drawScreen(controlScreen, x, y, width, height) --> Draws the screen content in the given area
 *   and returns an array of Clickable objects.
 */
var Screens = {
	
};